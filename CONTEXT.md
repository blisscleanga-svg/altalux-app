# AltaLux App — Contexto del Proyecto
> Última actualización: 2026-09-02

## ¿Qué es esto?
App de field service para AltaLux Mobile Detail (Roswell, GA).
Replica funcionalidad core de Urable pero custom, sin mensualidad.
Eventualmente se adaptará también para BlissClean Mobile Detail.

## Marcas
- **AltaLux Mobile Detail** — Roswell, GA (marca principal de este repo)
- **BlissClean Mobile Detail** — Dunwoody, GA (Fase 6, no iniciada)

## Stack
- Frontend: HTML/CSS/JS vanilla (sin frameworks)
- Base de datos: Supabase (PostgreSQL + Auth + Edge Functions) — **conectado y en uso real**
- Pagos: **Square** (Web Payments SDK + Payments API) — no Stripe
- Emails: Resend — no conectado (placeholder)
- SMS: Twilio — Toll-Free **APROBADO** (`+1 888-330-0491`), `business_settings.twilio_phone` seteado; `twilio_enabled` en `false` a propósito (pausado por el controlador hasta revisar el fix de seguridad del 2026-09-02)
- Hosting: Hostinger

## Fases del Proyecto
1. Booking widget con depósito (Square)
2. Panel admin (reservas, jobs, clientes)
3. App del técnico (mobile-optimized)
4. Invoicing y cobro del balance
5. SMS/Notificaciones automáticas
6. Adaptar para BlissClean (multi-tenant)

## Fase Actual
- FASE 0 — Setup ✅
- FASE 1 — Booking Widget ✅ completo, con cobro real vía Square
- FASE 2 — Panel Admin ✅ completo, conectado a Supabase real (ya no es memoria de sesión)
- FASE 3 — App del Técnico ✅ completo, login real por empleado, jobs asignados por `assigned_to`
- FASE 4 — Invoicing ✅ completo: invoices reales, envío por email (Resend) funcional (`invoice_link`, `payment_receipt`), trazabilidad completa de pagos (link abierto/iniciado/completado, tarjeta usada, notificación al admin) — corregida la línea de estado vieja de este archivo, que decía incorrectamente que el email era un placeholder (dejó de serlo desde el 2026-07-15, esto solo no se había actualizado acá)
- FASE 5 — SMS/Notificaciones 🟡 Sub-fase 1+2 (envío `booking_confirmation` + Message Center bidireccional) completa en código; Toll-Free ya aprobado, pero `twilio_enabled = false` a propósito hasta que el controlador revise el fix de seguridad del code review final (2026-09-02) y redespliegue — Sub-fases 2–4 (deep-link job detail, On My Way, campañas) no iniciadas
- FASE 6 — BlissClean multi-tenant ⬜ no iniciada

## Repositorio
- GitHub: `blisscleanga-svg/altalux-app` (rama `main`)
- Autenticación para push: Personal Access Token (no hay `gh` ni credencial guardada en el entorno de trabajo)

## booking/index.html — Booking Widget (Fase 1)
Widget de reserva público, un solo archivo HTML/CSS/JS vanilla, mobile-first.

**Flujo multi-step:** Service → Details → Schedule → Review & Pay

- **Service**: 3 categorías (Full Detail con paquetes Essential/Premium, Interior Only, Exterior Only), chips de vehículo con precio, add-ons organizados en 3 grupos (Interior, Machine Applied Wax con precio por tier de vehículo, Exterior), sidebar sticky con resumen en vivo (desktop).
- **Details**: datos del cliente (nombre, teléfono, email, dirección, vehículo) + checkboxes de notificaciones pre-marcados. Campo de dirección (`#c-address`) con autocompletado de Google Places (fallback a texto plano si Maps falla).
- **Schedule**: calendario inline (Lunes–Sábado, domingo deshabilitado), horarios 8am–4pm.
- **Review & Pay**: resumen completo, depósito 25% destacado en naranja, balance 75% en gris, política de cancelación, formulario de tarjeta con **Square Web Payments SDK** → tokeniza → Edge Function `square-payment` cobra el depósito → se guarda el booking en Supabase (`bookings`, incluyendo `square_payment_id`).

Todo el catálogo de precios (categorías/paquetes/vehículos) vive hardcodeado en el `<script>` de este archivo.

## admin/index.html — Panel Admin (Fase 2)
Un solo archivo HTML/CSS/JS vanilla. **Login real por empleado vía Supabase Auth** (`signInWithPassword`) — ya no hay password compartida hardcodeada ni cuenta maestra embebida en el código.

**Vistas (nav + sidebar):** Calendar, Jobs, Proposals, Customers, Payments.

- **Calendar**: mini-calendario + panel con 4 pestañas (Agenda/Week/Month/Day) + barra de navegación de periodo con ingresos y cantidad de jobs. Agenda agrupa jobs por día con ingresos diarios; Week es la grilla de slots (Lun–Sáb) con chips de jobs/eventos; Month muestra hasta 3 pills por día (puntos de color en móvil) y navega a Day al hacer click; Day es un timeline de una columna con los 6 slots fijos de horario. Los 4 KPI cards de arriba (Today's Revenue/This Week/Jobs Today/Pending) ahora sí se calculan en vivo (antes estaban hardcodeados en $0.00/0). Filtro por empleado (`#calendar-employee-filter`) aplica a las 4 vistas. El modal de detalle de job (admin y technician) tiene un panel "Share Payment Link" (Copy Link + Share vía Web Share API) que solo aparece si la invoice ya fue enviada (`job.invoice.publicToken` + `sentAt`).
- **Jobs**: tabla completa, status editable inline (Pending/Confirmed/In Progress/Completed), modal de detalle con historial de pagos.
- **Proposals**: cotizaciones previas a un job (Draft/Sent/Accepted/Declined), botón "Convert to Job" que crea un job real.
- **Customers**: derivados de los jobs + clientes standalone creados manualmente; perfil con tabs Overview/Job History y lifetime value. Modal "New Customer" con toggle Person/Business, múltiples direcciones/teléfonos/emails con label (Home/Work/Other), autocompletado de Google Places en direcciones (con fallback a texto plano si Maps falla — key restringida por dominio en Google Cloud). El perfil de cliente muestra estos datos en un bloque "Contact Info" dentro de Overview, separado de la pestaña "Addresses" (que es historial de direcciones de jobs, no datos de contacto). Columnas nuevas en `customers`: `customer_type`, `business_name`, `last_name`, `addresses`/`phones`/`emails` (jsonb) — las columnas viejas (`full_name`/`phone`/`email`/`address`) se mantienen por compatibilidad, siguen alimentando el resto de la app (Jobs/Payments/Invoices, todo sigue keyed por `email`).
- **Payments**: stats (Total Collected, This Month, This Week, Outstanding, Pending Deposits), filtros (fecha/status/cliente/método), tabla enriquecida, sistema de invoices (INV-00X, estados Draft/Sent/Paid/Overdue, tax toggle 6%, print/PDF), modal "Record Payment" con cobro real vía **Square Reader** (tarjeta presente) o **Square Link** (link de pago remoto), además de métodos manuales.
- **Empleados**: alta/gestión de empleados; crear login o resetear password pasa por la Edge Function `manage-employee-auth` (solo Owner, usa service role key server-side — el password nunca se guarda en una tabla legible por el cliente).
- **Botón flotante (FAB)**: centrado abajo, crea rápido: Quote/Job, Proposal, Event, Customer, Payment (con selector de job). El campo de dirección del modal "Quote/Job" (`#cj-address`) también tiene autocompletado de Google Places.
- **Realtime**: alertas en vivo de nuevos bookings entrantes.

**Backend real:** todo (jobs, payments, proposals, events, customers, employees) vive en Supabase — ya no es memoria de sesión.

## technician/index.html — App del Técnico (Fase 3)
App mobile-optimized para el equipo de campo.

- Login real por empleado (mismo modelo de Supabase Auth que admin).
- "Mis jobs de hoy" filtrado por la columna `assigned_to` en `jobs`.
- Registro de `start_time` / `end_time` al iniciar/terminar un job (sincronizado a Supabase).
- Alertas realtime de jobs asignados.

## Fixes recientes
- **2026-09-02 (Task 4 — Jobs mobile UX, Fase 5):** **La vista de Jobs del admin dejó de ser una tabla horizontal-scroll y pasó a ser una lista de tarjetas compactas y tappable, con tabs de status + toggle de "solo online bookings"** — el cambio central de todo el plan (Tasks 1-3 solo prepararon el terreno: columna `jobs.source`, campo `j.source` en memoria, y el componente `buildJobCardCompactHtml(j)` sin usar todavía). Reemplazado el `<table class="data-table">` de `#view-jobs` por `<div class="job-status-tabs" id="jobs-status-tabs">` (6 tabs: All/Pending/Confirmed/In Progress/Completed/Cancelled, cada uno con su contador) + botón `#jobs-online-toggle` ("🌐 Online Bookings") + `<div id="jobs-cards-list">`. `renderJobsTable()` reescrita: sigue respetando el filtro de cliente existente (`state.customerFilterEmail`, banner "Showing jobs for..."), calcula los contadores de cada tab **antes** de aplicar el filtro de tab/toggle (a propósito — así el contador de una tab no salta al cambiar a otra), y aplica ambos filtros nuevos en conjunto — `state.jobsStatusFilter` (uno de los 6 valores, default `'all'`) y `state.jobsOnlineOnly` (boolean, default `false`, filtra por `j.source === 'online_booking'`) — nunca uno pisando al otro. Pinta con `buildJobCardCompactHtml` (Task 3) o un empty-state si el filtro no matchea nada. Click en una `.job-card-compact` abre el modal de detalle del job existente (`openJobModal`, mismo patrón que los viejos botones "View"/`.job-view-btn`, agregado como primer caso del bloque de delegación de clicks a nivel documento para que tenga prioridad). CSS de los tabs agregada junto a las reglas `.pm-method-btn` existentes, reusando `var(--gold)`/`var(--line)`/`var(--ink-muted)` — sin colores nuevos. **Verificado con Playwright headless** (archivo real servido sobre origen `https://` falso vía `page.route`, internals expuestos solo en la copia en memoria vía `window.__test`): sembrados 8 jobs falsos (mezcla de los 5 status y de `source: 'manual'`/`'online_booking'`) → (1) 6 tabs con los contadros correctos (All 8, Pending 1, Confirmed 2, In Progress 1, Completed 3, Cancelled 1); (2) click en tab "Completed" → solo 3 tarjetas quedan, todas `data-status="completed"`, la tab gana `.active` y "All" la pierde; (3) click en el toggle online, combinado con la tab "Completed" ya activa → queda 1 sola tarjeta (la única Completed+online_booking), toggle gana `.active`; (4) click en una tarjeta → `#modal-overlay` pasa de `hidden` a visible y el título muestra el número de job correcto; (5) a 375×812 (iPhone) con la lista completa (8 tarjetas) → `document.documentElement.scrollWidth` = 375, sin overflow horizontal; (6) a 1400×900, mismo render, mismas 8 tarjetas y 6 tabs, cero errores de consola nuevos — desktop usa exactamente el mismo markup/función, no hay code path separado. Cero errores de consola en toda la corrida. **No desplegado** — queda pendiente del zip de Hostinger junto con el resto de esta fase de Jobs mobile UX (Tasks 5-6 del plan siguen).
- **2026-09-02 (Task 1 — Jobs mobile UX, Fase 5):** Agregada columna `jobs.source` (text, default `'manual'`, CHECK constraint) — primer paso de la redeseña del panel de Jobs móvil. Migración `20260902200000_jobs_source_column.sql` aplicada y verificada en producción. Ver spec en `docs/superpowers/specs/2026-09-02-jobs-mobile-ux-design.md`.
- **2026-09-02 (chat deep-link + Send via SMS de invoices):** Dos features bounded, aprobadas en chat sin spec. (1) **Deep-link de chat**: botón 💬 junto al teléfono en el modal de detalle del job (`admin/index.html`) — abre el hilo del Message Center para ese cliente (normaliza el teléfono a E.164 client-side con `toE164Client()`). Corregido de paso un bug real: `openMessageThread()` no hacía nada si el cliente nunca había tenido mensajes (`if (!thread) return;`) — ahora abre un hilo vacío listo para escribir el primer mensaje. (2) **"Send via SMS" de invoices**: nueva action `invoice_link` en `send-sms` (exige empleado autenticado, mismo patrón que `manual_reply` — el link/monto/etc. vienen del cliente porque quien llama ya es un empleado real, igual que `send-email`'s `invoice_link` siempre ha hecho). Activado el toggle Email/SMS/Both (antes solo Email seleccionable, SMS/Both "Coming soon" sin lógica) en los 3 puntos de entrada que existen: `admin/index.html` tiene un solo `sendPayableInvoice()` compartido entre el modal "Send Invoice" y el panel "Collect Payment → Send Payable Invoice" (arreglarlo una vez cubrió los dos); `technician/index.html` tiene su propia copia paralela `sendPayableInvoiceTech()` (duplicación ya existente en el proyecto, no tocada). Verificado con Playwright (técnica de servir el archivo real sobre un origen falso con una función expuesta solo para el test, nunca commiteada — mismo patrón ya usado y aprobado en el fix de Task 7): deep-link abre hilo vacío y hilo con mensajes reales correctamente, toggle Email/SMS/Both cambia selección y validación (warning de "sin teléfono"/"sin email" según el canal elegido) en los 3 lugares, sin errores de consola nuevos. Desplegada la función `send-sms` (`deno check` limpio, curl confirmó que `invoice_link` rechaza sin auth real) y subido el zip a Hostinger — Luis lo subió directo por el file manager mientras la API de deploy seguía intermitente. Verificado en vivo (`app.altaluxdetail.com/admin/`): el 💬 y el toggle Email/SMS de invoices ya están ahí.
  - **Bug reportado por Luis tras el deploy** (no relacionado con este cambio): "This job is not fully synced to the database" al enviar invoice de un job real (Luis Pabon, job #23) — verificado en Supabase que el job SÍ tiene `id`/`customer_id` reales; era el `JOBS` en memoria del navegador desactualizado (admin abierto desde antes de que existiera ese job). Se resolvió con un refresh manual, pero expuso un gap real: solo `bookings`/`payment_events`/`sms_messages` tienen suscripción realtime — `jobs`/`customers`/`invoices` solo se recargan al hacer login. Invisible en un tab de escritorio que uno refresca por costumbre; el plan real de Luis es anclar la webapp a la pantalla de inicio del iPhone, donde eso nunca pasa solo.
  - **Fix — auto-refresh al recuperar foco**: nuevo listener de `visibilitychange`/`focus` en `admin/index.html` que llama `loadAllDataFromSupabase()` + `refreshMessagesFromSupabase()` cada vez que la pestaña/webapp anclado vuelve a primer plano (con throttle de 15s para no golpear la base de datos si el usuario cambia de app rápido varias veces). No dispara si no hay sesión (`db` nulo) ni en la vista de técnico. Verificado con Playwright: cero llamadas antes de login, exactamente 1 al recuperar el foco, 0 adicionales si se repite de inmediato (throttle). **Pendiente de deploy** — todavía no se subió este cambio puntual (el zip que Luis subió es de antes de este fix).
- **2026-09-02 (fix del formulario de contacto de altaluxdetail.com):** El sitio de marketing (carpeta separada, fuera de este repo — `C:\Users\ledua\OneDrive\Escritorio\AltaLux Detail\website`) tenía un formulario de contacto que era pura simulación de UI: el JS hacía un `setTimeout` y mostraba "¡Enviado!" sin mandar nada a ningún lado. Agregado un nuevo action `website_contact` a la Edge Function compartida `send-email` (`supabase/functions/send-email/index.ts`) — arma un email con nombre/email/vehículo/servicio/dirección/notas y lo manda a `business_settings.notification_email` (`altaluxdetail@gmail.com`) vía Resend, mismo patrón que `internal_notification`/`payment_notification_admin`. El formulario del sitio ahora hace un `fetch` real a esta función (mismo `SUPABASE_ANON_KEY` público que ya usa `booking/index.html`), con un honeypot (`#hp-website`) para descartar spam de bots en silencio, y muestra un mensaje de error real (con teléfono/email de respaldo) si el request falla, en vez de fingir éxito. **Nota sobre esta sesión:** hubo una colisión con la otra sesión de Claude Code activa en esta máquina — el primer intento de este cambio se perdió sin comitear cuando esa sesión tocó el working tree (aparentemente durante su propio trabajo de SMS de Sub-fase 1+2, ver entradas de abajo); se reaplicó y se comiteó de inmediato esta vez. Desplegado con `supabase functions deploy send-email` (`deno check` limpio antes de cada deploy).
- **2026-09-02 (deploy final de SMS Sub-fase 1+2 — ya en producción):** Desplegados `send-sms` y `twilio-webhook` (con el fix de seguridad de abajo), verificado en vivo que el relay abierto quedó cerrado (`manual_reply` sin auth → 401 real contra la función desplegada). Subido el zip de Hostinger con `admin/index.html`/`booking/index.html` actualizados — verificado en vivo (`app.altaluxdetail.com/admin/` muestra "Messages" en el nav, `app.altaluxdetail.com/booking/` carga bien). Reactivado `business_settings.twilio_enabled = true` para `altalux` (estaba pausado desde la mitigación de emergencia). Toll-Free number `+18883300491` aprobado y activo. **Pendiente como verificación manual real:** hacer un booking real con `#c-sms` marcado y confirmar que llega el SMS, responder desde el celular y confirmar que aparece en el Message Center, y un round-trip real de STOP/START para validar que no choca con el manejo de opt-out a nivel carrier de Twilio (ver hallazgo #4 más abajo).
- **2026-09-02 (code review final de toda la rama SMS Sub-fase 1+2 — fix consolidado):** revisión final de la rama completa (no de tarea en tarea) encontró 1 hallazgo CRÍTICO, 3 IMPORTANTES y 3 menores. Todos arreglados en un solo pase. **Estado REAL de Twilio ahora mismo: el número Toll-Free YA FUE APROBADO, `business_settings.twilio_phone = '+18883300491'`, pero `twilio_enabled` está en `false` a propósito** — el controlador lo apagó como mitigación de emergencia del hallazgo #1 y lo volverá a encender él mismo después de revisar este fix. Las entradas de Task 7/Task 8 más abajo dicen que `twilio_phone`/`twilio_enabled` siguen en `null`/`false` "esperando aprobación": eso ya no es cierto, esta entrada manda.
  - **#1 CRÍTICO — `send-sms` era un relay de SMS abierto.** No autenticaba al llamador más allá del anon key (público, embebido en el cliente), así que cualquiera podía POSTear un `manual_reply` con destinatario y cuerpo arbitrarios (1600 chars), o abusar `booking_confirmation` con `customerName`/`service`/`date`/`deposit`/`balance` y destinatario todos suministrados por el atacante. Arreglado partiendo el modelo de confianza en dos: (a) **`manual_reply` ahora exige un empleado autenticado real** — `Authorization: Bearer <token>` → `admin.auth.getUser()` → lookup en `employees` por email, exigiendo `is_active !== false` **y** `employee.business_id === businessId` del payload (un empleado de un negocio no puede mandar como otro). Cualquier rol activo puede responder (no solo Owner — Managers/Técnicos también contestan clientes a diario). 401 si falta/es inválido el token, 403 si no es empleado activo de ese negocio. Mismo patrón que `manage-employee-auth`. `admin/index.html` ahora manda `session.access_token` en vez del anon key. (b) **`booking_confirmation` dejó de confiar en el `data` del cliente**: el contrato nuevo es `{ action, businessId, data: { bookingId } }` — solo el id. La función re-lee la fila real de `bookings` con service role (filtrando por `id` **y** `business_id`) y arma el mensaje entero desde las columnas de esa fila (`full_name`, `phone`, `category`, `service_date`, `service_time`, `deposit`, `total`). Si `bookings.receive_reminders` es falsy, no manda: esa columna es ahora la **fuente de verdad** del consentimiento, no el `if (state.smsReminders)` del cliente (que se queda como primera compuerta). Además la fila tiene que traer `square_payment_id` no vacío (si no, `{skipped: true, reason: 'no_payment_recorded'}`): como `bookings` tiene policy de INSERT para `anon`, "existe una fila real" solo no alcanza — cualquiera puede insertar una. Exigir un pago registrado sube la vara a que la reserva haya pasado por el flujo de pago. **No lo cierra del todo** (no se verifica ese id contra Square, así que un atacante que sepa falsificar justo ese campo sigue pasando); riesgo aceptado, razonable junto al rate limit y el opt-out para un endpoint que por diseño no tiene llamador que autenticar. Sigue siendo llamable con el anon key porque el widget de booking es legítimamente anónimo — el fix acá es "el servidor deriva el contenido de una fila que él controla", no auth de llamador. (c) **Rate limit** barato por `(business_id, phone)`: 5 mensajes en 5 minutos; pasado eso devuelve `{skipped: true, reason: 'rate_limited'}` en silencio en vez de un error ruidoso (no le avisa al que está sondeando el límite, y 5/5min no rompe una conversación real de ida y vuelta).
  - **Nota de implementación de (b):** `booking/index.html` no puede leer de vuelta el id que genera la base — `bookings` tiene policy de INSERT para `anon` pero **ninguna de SELECT**, así que `.insert().select('id')` falla con RLS `42501` (verificado empíricamente contra una tabla de prueba con la misma forma). Por eso `buildBookingPayload()` ahora genera el uuid en el cliente (`newBookingId()`, `crypto.randomUUID()` con fallback RFC 4122 v4) y lo inserta explícito. La llamada a `send-email` quedó **exactamente igual** (sigue mandando `commonData`); solo cambió `sendSmsAction`.
  - **#2 IMPORTANTE — el Message Center del admin nunca se refrescaba sin recargar la página.** `MESSAGES` solo se poblaba en `loadAllDataFromSupabase()` (login y evento realtime `payment_completed`). Agregada `subscribeToSmsMessages()` (canal `altalux-sms-messages`, INSERT en `sms_messages` filtrado por `business_id`, empuja a `MESSAGES`, re-renderiza si la vista Messages está activa, y toastea "New message from …" en los inbound), llamada junto a `setupRealtimeBookingsSubscription()`/`subscribeToPaymentEvents()`. Además `switchView` pasó a `async` y re-fetchea al entrar a Messages (fallback por si se cayó el canal realtime), vía la nueva `refreshMessagesFromSupabase()`. El select de `sms_messages` pasó de ascendente sin límite a `.order('created_at', { ascending: false }).limit(500)` + `reverse()` en JS — ascendente con tope de filas escondería justo los mensajes más nuevos. Y se agregó el ítem "Messages" al bottom-nav móvil, que Task 5 se había salteado.
  - **#3 IMPORTANTE — la resolución de negocio del webhook entrante no normalizaba formatos.** `twilio-webhook` hacía `.eq('twilio_phone', to)` contra un valor tipeado a mano; con un espacio de más o un formato no-E.164, **todo mensaje entrante se caía en silencio** (200, TwiML vacío, una línea de consola, cero señal para el operador). Ahora trae las filas de `business_settings` con `twilio_phone` no nulo, normaliza cada una con el helper `toE164()` que ya existe en ese archivo y compara en JS contra `toE164(to)` — mismo enfoque que ya usaba `findMatchingCustomer` para teléfonos, y es una tabla de una fila por negocio. El log de "no encontré negocio" ahora incluye los candidatos. **A propósito NO se filtra por `twilio_enabled`:** `twilio_phone` ya es único por negocio, así que ese filtro no aportaba corrección y sí un footgun — mientras un negocio está pausado (`twilio_enabled = false`) su número sigue vivo en Twilio, y los entrantes (STOP sobre todo) tienen que seguir honrándose y logueándose. Una pausa es respuesta a incidente del lado **saliente**; tirar los entrantes durante una pausa es exactamente al revés.
  - **#4 IMPORTANTE — el opt-out a nivel carrier de Twilio podía desincronizarse de `sms_opt_outs`.** Si Twilio maneja STOP en el número/carrier por su cuenta (común en toll-free verificados), un envío posterior devuelve el error `21610` aunque nuestra tabla no se haya enterado. Ahora `sendViaTwilio` lanza un `TwilioError` que conserva el `code` del body, y si es `21610` se hace upsert de `sms_opt_outs` con `opted_out_at = now()` antes de propagar el error — la lista local se auto-cura desde la señal del propio Twilio. (El round-trip real de STOP/START sobre el número vivo queda como verificación manual del controlador.)
  - **#5 menor — guarda de re-entrancy en el envío de reply.** El handler de `#sms-reply-send` no tenía nada contra un doble click: ahora deshabilita el botón al entrar (con el "Sending..." que ya existía) y lo re-habilita en un `finally`. Verificado con Playwright: 3 clicks seguidos → **1 sola** request HTTP.
  - **#6 menor — default inseguro del flag de consentimiento.** `state.smsReminders` arrancaba en `true` en `booking/index.html` (inalcanzable en la práctica, porque `#c-sms` está desmarcado y `collectFormData()` lo sobreescribe antes de que importe) — pasó a `false` por principio.
  - **#7 menor — la migración no era re-ejecutable.** `CREATE POLICY` no tiene forma `IF NOT EXISTS`, así que re-correr `20260902120000_sms_messaging.sql` tras un fallo parcial reventaba. **No se editó esa migración ya aplicada y commiteada**; se creó `20260902190000_sms_messaging_idempotency_fix.sql` con `DROP POLICY IF EXISTS` + `CREATE POLICY` para las dos policies, y de paso recrea la FK `sms_messages_customer_id_fkey` (nombre real confirmado con `pg_constraint`) como `ON DELETE SET NULL`, para que borrar un cliente no falle y el historial de SMS sobreviva sin el vínculo. Aplicada con `supabase db push` y verificada (`confdeltype = 'n'`, ambas policies presentes).
  - **Verificación:** `deno check` limpio en ambas funciones. Los rechazos de auth probados con curl contra una instancia local de `send-sms`: anon key sola → **401**, token basura → **401**, JWT manipulado → **401**, sin header → **401** (ninguno 500). El contrato de `booking_confirmation` probado contra un stand-in local de PostgREST: payload viejo del exploit (sin `bookingId`) → 400; `bookingId` inexistente → 404; `receive_reminders = false` → `{"skipped":true}`; y con `bookingId` válido **más** campos de atacante mezclados, la fila que se loguea prueba que destinatario y cuerpo salieron de la fila de la base (`+15550001111`, "Real Customer From DB", balance $150.00 = total − deposit) ignorando por completo lo que mandó el atacante. Rate limit y auto-cura del 21610 también ejercitados con mocks. `admin/` y `booking/` verificados con Playwright headless sobre las páginas reales servidas por HTTP: 0 errores de consola, Messages en el bottom-nav, `switchView('messages')` dispara el select `created_at.desc&limit=500`, el hilo se ve en orden cronológico tras el `reverse()`, el botón de reply se deshabilita/rehabilita, el `Authorization` que sale ahora es el token de sesión (no el anon key), `#c-sms` desmarcado por default y `state.smsReminders: false`. **No se desplegó nada ni se subió zip — eso queda del lado del controlador.**
- **2026-09-02 (Task 8 — cierre de Sub-fase 1+2):** **SMS Sub-fase 1+2 (Fase 5) queda completa y desplegada del lado del código; solo falta la aprobación del número Toll-Free de Twilio para que funcione de punta a punta.** Resumen de todo el esfuerzo (Tasks 1–8, 8 commits en `main`):
  - **Schema** (`20260902120000_sms_messaging.sql`): tablas `sms_messages` (log bidireccional, `direction`/`status`/`action`/`read`) y `sms_opt_outs` (compliance STOP/START), columna `sms_toggles jsonb` en `business_settings`. RLS: SELECT/UPDATE para `authenticated` del mismo negocio, sin policy de INSERT (todos los writes vienen de Edge Functions con service-role key).
  - **Edge Functions, ambas creadas, type-checked y DESPLEGADAS a producción:** `send-sms` (outbound — `booking_confirmation` templado + `manual_reply` raw, valida E.164, chequea opt-out, integra con la API de Twilio, loguea a `sms_messages`) y `twilio-webhook` (inbound — público sin JWT porque Twilio no puede mandar tokens, valida firma HMAC-SHA1 `X-Twilio-Signature`, maneja STOP/START/HELP con respuesta TwiML automática, loguea todo a `sms_messages`). Smoke tests post-deploy pasaron (error de acción desconocida en `send-sms`, 403 en `twilio-webhook` sin firma válida).
  - **Message Center en `admin/index.html`:** nav "Messages" (solo Owner/Manager, mismo patrón que Payments) → tabla de conversaciones agrupadas por teléfono con indicador de no-leídos → modal de hilo (burbujas inbound/outbound, reply real vía `send-sms` con `action: 'manual_reply'`, marca inbound como leído al abrir). Todo el contenido interpolado pasa por `escapeHtml()`.
  - **`booking/index.html` wired:** dispara `sendSmsAction('booking_confirmation')` (mismo patrón que el email de confirmación) solo si el cliente marcó el checkbox de consentimiento SMS (`#c-sms`, sin premarcar por default — requisito de Twilio) — verificado con Playwright que 0 requests salen si está desmarcado y 1 request a `/functions/v1/send-sms` si está marcado.
  - **Verificación:** todo lo verificable sin credenciales reales de Twilio ni sesión de admin autenticada (arquitectura del código, escaping, guardas de opt-in, smoke tests de las Edge Functions ya desplegadas, comportamiento del checkbox con Playwright headless) fue confirmado en cada task. Ver las entradas de Tasks 1–7 abajo para el detalle completo de cada pieza.
- **2026-09-02 (Task 2 — Jobs mobile UX, Fase 5):** Tagging de jobs creados desde bookings con `source: 'online_booking'`. Task 1 agregó la columna `jobs.source` (default `'manual'`); Task 2 hace que el flujo de conversión de bookings a jobs ahora explícitamente sete `source: 'online_booking'` en el insert (`admin/index.html:4376`), y la función `mapJobRow()` ahora mapea esa columna al objeto en memoria como `source: row.source || 'manual'` (`admin/index.html:4039`). Task 4 (no esta) filtrará en la vista móvil de Jobs para distinguir jobs manuales de los que vinieron de online bookings. Cambios verificados con `grep`: `source: 'online_booking'` aparece exactamente 1 vez (el insert), `source: row.source || 'manual'` aparece exactamente 1 vez (el mapping).
  - **⚠️ PENDIENTE — bloqueante para SMS real:** el número Toll-Free de Twilio sigue en cola de aprobación. Hasta que se apruebe, `business_settings.twilio_phone`/`twilio_enabled` quedan en `null`/`false` para `altalux` — ningún envío ni recepción de SMS real funciona todavía, aunque todo el código/infraestructura está listo. Una vez aprobado: (1) `supabase secrets set TWILIO_ACCOUNT_SID/TWILIO_AUTH_TOKEN/TWILIO_WEBHOOK_URL`, (2) `UPDATE business_settings SET twilio_phone = '...', twilio_enabled = true WHERE business_id = 'altalux'`, (3) configurar esa URL como webhook de mensajes entrantes en la consola de Twilio, (4) probar un booking real con `#c-sms` marcado y confirmar visualmente el Message Center en `app.altaluxdetail.com/admin/`.
  - **⚠️ PENDIENTE — no iniciado, fuera de alcance de esta entrega:** Sub-fase 2 (deep-link a chat SMS desde el detalle de un job), Sub-fase 3 ("On My Way" con geolocalización/ETA, como en Urable), Sub-fase 4 (campañas/broadcast manual a múltiples clientes) — quedan para una fase futura, según el spec original de Fase 5.
  - **Deploy:** zip `altalux-hostinger-2026-09-02.zip` reconstruido con los mismos 10 archivos del zip anterior (`admin/index.html` y `booking/index.html` ahora cargan los cambios de esta sub-fase) — subida a Hostinger deferida al controlador, pendiente confirmación explícita del usuario antes de sobreescribir el sitio en vivo.
- **2026-09-02 (Task 3 — Jobs mobile UX, Fase 5):** Nuevo componente `buildJobCardCompactHtml(j)` (`admin/index.html`, justo antes de `canEditJobs()`) — tarjeta compacta y tappable para la lista de Jobs (patrón de Urable: solo número de job, servicio, pin de dirección, nombre de cliente, saldo pendiente y badge de estado; el resto — dirección completa, desglose de depósito, vehículo — se queda en el modal de detalle). No reemplaza `buildJobCardHtml()`, que Agenda sigue usando tal cual (a propósito, según Global Constraints del plan). CSS agregada inmediatamente después de las reglas `.job-card[data-status=...]` (`admin/index.html:~802`), reutilizando los mismos valores de color por estado (`#90a4ae` pending, `var(--blue-light)` confirmed, `var(--orange)` inprogress, `var(--green)` completed, `var(--danger)` + `opacity:.7` cancelled) — sin inventar colores nuevos. Todo el texto derivado de datos del job (`jobNumber`, `service`, `customer.name`) pasa por `escapeHtml()`. **Verificado con Playwright headless** (archivo real servido sobre origen `https://` falso vía `page.route`, función expuesta en `window.__test` solo en la copia en memoria): job normal → `data-status="confirmed"`, `data-job-id` correcto, título/cliente/monto presentes y con el pin 📍 (hay `address`); job "XSS" con `service: '<script>...</script>'` y `customer.name` con `<img onerror=...>` → el conteo de `<script>` en el DOM no creció, ni `window.__xss_fired` ni `window.__xss_img` se dispararon, y el HTML interno contiene literalmente `&lt;script&gt;`/`&lt;img` (texto inerte, no ejecutado). **⚠️ Componente aún no usado en ningún lado** — Task 4 (siguiente, no esta) lo conecta desde la vista de Jobs reconstruida con delegación de clicks sobre `data-job-id`.
- **2026-09-02 (Task 7):** **Wiring de SMS en el booking widget (`booking/index.html`)** — cierra la parte de wiring para el cliente (booking + admin). Agregada constante `SEND_SMS_FUNCTION_URL` (`/functions/v1/send-sms`) al lado de `SEND_EMAIL_FUNCTION_URL`. Nueva función `sendSmsAction(action)` que espeja exactamente el patrón de `sendEmailAction` (mismo fetch, mismo `Authorization`/`apikey` headers, mismo `.catch()` no-bloqueante). Agregada la llamada condicional `if (state.smsReminders) sendSmsAction('booking_confirmation');` justo después de las 2 llamadas de email, garantizando que SMS solo se dispara si el checkbox `#c-sms` (consentimiento requerido por Twilio, sin marcar por default desde Task 1) está marcado. **Verificado con Playwright headless + real network interception** (`verify-sms-real.js`): real file content served over fake `https://` origin (defeats `isLocalPreview()` check legitimately — the app's own payment-safety guard), real DOM clicks through Service → Details (sets state.smsReminders from real `#c-sms` checkbox) → Schedule → instead of unreachable Pay button (architecturally blocked by `isLocalPreview()` in ANY non-production environment by the app's own design, not fixable without production HTTPS), direct invocation of real unmodified `triggerBookingEmails()` via one-line exposure hook. Result: `#c-sms` unchecked → `[]` send-sms requests, `#c-sms` checked → `["https://xmhsehfdmiqbwhpqjgon.supabase.co/functions/v1/send-sms"]`. La condicional `if (state.smsReminders)` actúa de guarda: estado.smsReminders=false (unchecked) → sendSmsAction() no se invoca → 0 requests; estado.smsReminders=true (checked) → sendSmsAction() se invoca → fetch a `/functions/v1/send-sms` se dispara. Payload real: `{ action: 'booking_confirmation', businessId: 'altalux', data: commonData }` (commonData poblada por estado real del checkout, incluye `customerPhone`). **Esto completa la Sub-fase 1+2 (admin UI + booking UI + ambas funciones Edge)** — solo queda Task 8 (verificación final de deploy).
- **2026-09-02 (Task 6):** **Modal de hilo de conversación SMS en `admin/index.html`** — cierra la parte de UI admin de la Sub-fase 1+2 (quedan Task 7 — booking widget — y Task 8 — deploy final — del plan de 8 tasks). Nuevo `<div class="modal-overlay" id="sms-thread-overlay">` insertado justo antes del modal `status-picker-overlay` (título del cliente, `#sms-thread-list` con burbujas de mensaje, textarea de reply, botón "Send"). Tres funciones nuevas dentro de la sección `MESSAGE CENTER (SMS)`, justo después de `renderMessagesView()`: `openMessageThread(phone)` (busca el thread vía `messageThreads()`, pinta una burbuja por mensaje ordenada cronológicamente — alineación/color distintos para inbound/outbound, **todo el body pasa por `escapeHtml()`** antes de insertarse en innerHTML — y marca como leídos solo los mensajes de ESE thread que son `direction: 'inbound'` y `!read`, vía `UPDATE ... WHERE id IN (unreadIds)` con `attemptSupabaseWrite`, nunca un UPDATE global), `closeMessageThreadModal()` (oculta el overlay, limpia el textarea/status, y resetea `currentThreadPhone = null` para que no queden datos del thread anterior si se abre otro), y el handler de click de `#sms-reply-send` (POST a `SEND_SMS_FUNCTION_URL` con `action: 'manual_reply'`, re-fetch de `sms_messages` del thread tras enviar, re-render). Verificado con Playwright headless contra una copia local (`window.__TEST_HOOKS__` solo en la copia de prueba): sembrado un thread con 3 mensajes (mezcla inbound/outbound, 2 inbound sin leer, uno con `<appointment>` en el body para probar escaping), click en "Open" → overlay pasa a visible, 3 burbujas en orden cronológico correcto, `<appointment>` aparece escapado (`&lt;appointment&gt;`) nunca como tag crudo, ambos inbound quedan `read: true` tras abrir (el outbound ya estaba `read: true`, sin tocar). Abierto un SEGUNDO thread (teléfono distinto) inmediatamente después: título y burbujas cambian al nuevo thread, `currentThreadPhone` se actualiza sin arrastrar el teléfono anterior, y el mark-as-read de este segundo thread no volvió a tocar los mensajes ya leídos del primero (confirmado leyendo el array `MESSAGES` completo al final). Botón "Close" oculta el overlay y limpia el estado. El envío real de reply (fetch a `send-sms`) no se puede probar en este sandbox `file://` (sin sesión de auth de Supabase — misma limitación documentada que cualquier otro write path del proyecto) — solo se confirmó que el botón existe y tiene su listener; se probará real una vez Twilio esté aprobado (Task 8).
- **2026-09-02 (Task 5):** **Messages — nav view + lista de conversaciones en `admin/index.html`** (solo la lista, sin el modal de detalle de hilo todavía — eso es Task 6). Nuevo botón "Messages" en nav desktop y sidebar (`data-view="messages"`), nueva sección `<section id="view-messages">` con tabla `.data-table` (`#messages-table-body`), agregado `'messages'` a `ROLE_PERMISSIONS.views` de Owner y Manager (Technician/otros roles no lo ven — mismo patrón que Payments), y `SEND_SMS_FUNCTION_URL` (constante, no usada todavía — la usará Task 6 para enviar replies). `loadAllDataFromSupabase()` ahora carga `sms_messages` completo (ordenado por `created_at`) al array module-level `MESSAGES` en cada refresh. Funciones nuevas: `messageThreads()` (agrupa `MESSAGES` por `phone`, calcula último mensaje/`unread`/nombre de cliente, ordena por mensaje más reciente — consumida por Task 6), `findCustomerNameByPhone()` (matchea por últimos 10 dígitos contra `STANDALONE_CUSTOMERS`/`JOBS[].customer`, cae de vuelta al teléfono crudo si no hay match), `renderMessagesView()` (pinta la tabla, un row por conversación, indicador 🔴 si hay mensajes inbound sin leer, botón "Open" que llama `openMessageThread(phone)` — esa función **no existe todavía**, la crea Task 6, así que el click no hace nada útil por ahora, esperado). Todo el contenido interpolado en el HTML de la tabla (teléfono, nombre de cliente, cuerpo del mensaje) pasa por `escapeHtml()` antes de insertarse. Verificado con Playwright headless contra una copia local del archivo (hook de test temporal `window.__TEST_HOOKS__` solo en la copia de prueba, nunca en el archivo real): sembradas 4 filas falsas de `sms_messages` (3 teléfonos distintos), click real en el botón de nav "Messages" → `#view-messages` gana `.active`, la tabla renderiza exactamente 3 filas (una por teléfono, agrupación correcta), cero errores de consola nuevos comparado contra una corrida baseline del archivo previo a este cambio (aparte del `ERR_FILE_NOT_FOUND` de `/shared/config.js` ya conocido y preexistente al probar por `file://`).
- **2026-09-02 (Task 4):** **`send-sms` y `twilio-webhook` desplegadas a producción** (`supabase functions deploy send-sms`, `supabase functions deploy twilio-webhook --no-verify-jwt` — ambos comandos reportaron éxito, `dashboard_url` confirmado). Smoke tests corridos, alcance reducido a lo que no requiere las credenciales reales de Twilio (esas quedan para el controlador, ver abajo):
  - `send-sms` — test de `nonsense_action`: **pasó exacto** (`{"error":"Unknown action: nonsense_action. Expected one of booking_confirmation, manual_reply."}`). Test de teléfono inválido (`"not-a-number"`): **no coincide con lo esperado en el brief** — devolvió `{"error":"Twilio is not configured/enabled for business \"altalux\"."}` en vez del error de normalización, porque el código (`index.ts` línea 149) chequea `biz.twilio_enabled`/`biz.twilio_phone` ANTES de validar el teléfono (línea 160-161) — como `business_settings.twilio_phone`/`twilio_enabled` todavía no están seteados (Step 2, pendiente), esa rama nunca se alcanza. No es un bug de esta tarea, es una divergencia del brief con el estado real del sistema en este momento del rollout — quedará resuelto solo una vez el controlador complete Step 2.
  - `twilio-webhook` — test de request forjado/sin firma: **pasó exacto** (`HTTP/2 403`, body `Forbidden`).
  - **Pendiente, deferido al controlador (fuera del alcance de este dispatch, requiere credenciales reales de Twilio que se mantienen fuera del contexto del subagente a propósito):** Step 2 completo (`supabase secrets set TWILIO_ACCOUNT_SID/TWILIO_AUTH_TOKEN/TWILIO_WEBHOOK_URL`, y `update business_settings set twilio_phone = '...', twilio_enabled = true where business_id = 'altalux'` — bloqueado además porque el número Toll-Free de Twilio sigue en cola de aprobación), y el sub-test de Step 4 de request correctamente firmado (HMAC-SHA1 con el `TWILIO_AUTH_TOKEN` real) que confirma el happy-path de inserción en `sms_messages`.
- **2026-09-02 (Task 3):** **Edge Function `twilio-webhook` creada y type-checked** — `supabase/functions/twilio-webhook/index.ts` implementada como endpoint público (sin verificación de JWT — Twilio no puede mandar tokens) que recibe POSTs de `application/x-www-form-urlencoded` cuando un cliente envía SMS a la línea toll-free del negocio. Valida cada request con HMAC-SHA1 contra el header `X-Twilio-Signature` (algoritmo exacto de https://www.twilio.com/docs/usage/security#validating-requests — se debe configurar bien porque acepta/rechaza mensajes reales). Responde con TwiML (`text/xml`). Soporta 3 palabras clave con respuesta automática: STOP/STOPALL/UNSUBSCRIBE/CANCEL/END/QUIT (marca como opt-out en `sms_opt_outs`, responde "You have been unsubscribed..."), START/UNSTOP/YES (marca como opt-in), HELP/INFO (help message con links/phone). Todos los mensajes (incluyendo replies automáticas) se loguean a `sms_messages` con `direction: 'inbound'`, matching del cliente por teléfono E.164 contra `customers(phone, phones[].number)` usando últimos 10 dígitos. Type-checked con Deno (parámetros de funciones actualizados a `any` para compatibilidad con Supabase client SDK, mismo patrón que `send-sms`). **No aún desplegado. No aún configurado como Twilio webhook URL** — ambos quedan para Task 5.
- **2026-09-02 (Task 2):** **Edge Function `send-sms` creada y type-checked** — `supabase/functions/send-sms/index.ts` implementada con soporte para 2 acciones (`booking_confirmation` templado, `manual_reply` raw body), validación de teléfono E.164 (10-dígitos US), opt-out check, integración real con API de Twilio (Basic Auth), y logging automático a `sms_messages` con dirección `outbound`. Maneja credenciales globales de Twilio (`TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN` como Supabase secrets) + número `From` per-negocio (`business_settings.twilio_phone`). Type-checked con Deno sin errores — archivo creado, no desplegado (Task 4). Formato de mensaje `booking_confirmation`: "Hi {customerName}, this is {businessName} confirming your {service} appointment on {date} at {time}. Deposit paid: {deposit}. Balance due at service: {balance}. Reply STOP to opt out." — todas las funciones (toE164, fmtCurrency, fmtDate) incluidas.
- **2026-09-02:** **Migración SMS Fase 5 Sub-fase 1+2 iniciada** — `20260902120000_sms_messaging.sql` creó tablas `sms_messages` (id, business_id, customer_id, phone, direction, body, twilio_sid, status, action, read, created_at) y `sms_opt_outs` (business_id, phone, opted_out_at con PK compuesta), más columna `sms_toggles jsonb` en `business_settings`. RLS habilitado: `SELECT`/`UPDATE` para `authenticated` del mismo negocio (via `current_business_id()`), sin policy de INSERT (todos los writes vienen de Edge Functions `send-sms`/`twilio-webhook` con service-role key). Verificado: 11 columnas de `sms_messages` en orden, `sms_toggles` retorna `{"booking_confirmation": true}` para `altalux`.
- **2026-09-01:** Arranca Fase 5 (SMS vía Twilio). Modelo de referencia: Urable (usan Twilio con número toll-free dedicado por negocio + Message Center bidireccional en el admin — inbox por cliente, campañas manuales, botón "On My Way" con geolocalización/ETA). Se decidió trabajar la Sub-fase 1+2 primero (envío saliente `booking_confirmation` + conversación bidireccional/webhook), dejando "On My Way" y campañas para después. Número: toll-free comprado directo en Twilio (no vía LeadConnector/GHL — ese número, `+1 888-853-0590`, seguía "Rejected" en la verificación de GHL). Mientras Luis completaba el registro Toll-Free en la consola de Twilio, se resolvieron 2 bloqueantes reales encontrados en el camino:
  - `privacy.html` y `terms.html` existían en el repo desde el 2026-08-05 pero **nunca habían sido incluidos en ningún zip de deploy a Hostinger** (confirmado con 404 en vivo) — Twilio/GHL los piden como URLs públicas para la verificación. Se agregaron al zip (ver [[altalux_deploy_workflow]]) y quedan en `https://app.altaluxdetail.com/privacy.html` / `.../terms.html` una vez subido el zip `altalux-hostinger-2026-09-01.zip`.
  - El checkbox de consentimiento SMS en `booking/index.html` (`#c-sms`, `#c-offers`, sección "Notifications") venía **pre-marcado** (`checked`) y sin frecuencia de mensajes, disclaimer de rates, instrucciones HELP/STOP, ni links a Terms/Privacy — Twilio exige explícitamente que el checkbox de opt-in no venga premarcado y que incluya esos elementos, o rechaza la verificación (como ya pasó una vez en GHL). Corregido: ambos checkboxes arrancan sin marcar, el label de `#c-sms` ahora incluye frecuencia (~2-4 msgs/booking), "Msg & data rates may apply", "Reply STOP to cancel, HELP for help", y links a Terms/Privacy. Verificado con Playwright headless contra el archivo local: ambos checkboxes `checked === false` por default, links resuelven a las URLs correctas, sin errores de consola nuevos.
  - Pendiente: una vez Luis suba el zip y la verificación Toll-Free de Twilio sea aprobada, retomar el spec de Sub-fase 1+2 (tabla de mensajes, edge function `send-sms`, webhook entrante, Message Center en el nav del admin).
- **2026-08-08 (4):** Luis reportó "cuando asigna el trabajo al empleado no ejecuta la asignación — solo queda reflejado en el panel de admin pero no envía los datos al empleado". Causa: asignar un técnico (checkbox rápido del modal de detalle, guardar "Edit Job", o crear un job nuevo) solo mutaba `job.assignedEmployeeIds` en memoria del navegador — **ningún** camino escribía la columna `jobs.assigned_to` en Supabase. El panel del técnico consulta exactamente esa columna (`technician/index.html`, filtro `eq('assigned_to', currentUser.supabaseId)`, ya correcto) — por eso se veía "asignado" en admin pero nunca llegaba. Corregido en los 3 puntos de `admin/index.html` (checkbox rápido del modal, guardado de "Edit Job", creación de job nuevo) — todos ahora incluyen `assigned_to: <supabaseId del primer empleado seleccionado>` en el insert/update real a Supabase, con el mismo patrón `attemptSupabaseWrite` ya usado en el resto del archivo.
- **2026-08-08 (3):** **Bug de arquitectura mucho más grave, encontrado mientras se probaba el fix de arriba contra el tenant de prueba `test-detailing-co`:** ningún tenant del sistema SaaS (aprobado vía `manage-tenant`/`platform/index.html`) puede crear clientes, jobs, pagos, vehículos, invoices, proposals ni events — **todo insert fallaba silenciosamente con RLS 42501**, tragado por el `catch` de `attemptSupabaseWrite` como "saved locally only". Causa raíz: `jobs`, `customers`, `vehicles`, `bookings`, `payments`, `invoices`, `employees` tienen `business_id ... DEFAULT 'altalux'` desde que la app era single-tenant (`phase_a_multitenant.sql`); `proposals`/`events`/`invoice_payments`/`invoice_refunds` tienen el mismo default literal en migraciones posteriores. El frontend nunca manda `business_id` explícito en la mayoría de sus ~12 `.insert(...)` — funcionaba de pura casualidad porque el default coincidía con el único tenant real que existía. Las políticas RLS agregadas en `security_rls_audit_part2.sql` (`WITH CHECK business_id = current_business_id()`) rompen esa casualidad para cualquier tenant que no sea `altalux`: cualquier negocio nuevo aprobado por el sistema SaaS quedaría con la app 100% inutilizable desde el primer intento de guardar algo.
  - **Fix**: nueva migración `20260808200000_fix_business_id_default_trigger.sql` — función `set_business_id_from_context()` + trigger `trg_set_business_id` `BEFORE INSERT` en las 11 tablas afectadas. Sobreescribe `NEW.business_id` con `current_business_id()` (resuelto por email del JWT contra `employees`) **solo cuando ese valor resuelve a algo** — para `anon` (booking público) y `service_role` (Edge Functions, incluida la creación del primer employee de un tenant nuevo en `approve_tenant`) `auth.jwt()->>'email'` es null, así que el trigger no interfiere y deja pasar el `business_id` explícito que esos caminos ya mandan correctamente. Para cualquier empleado autenticado normal, el insert queda automáticamente anclado a SU negocio real sin depender de que el frontend lo mande bien — bonus de seguridad: un usuario autenticado ya no puede insertar `business_id` de otro tenant aunque lo mande explícito en el payload.
  - **Verificado en producción real** (tenant de prueba `test-detailing-co`, sin tocar datos de `altalux`): insert de `customers` sin `business_id` en el payload —antes fallaba con 42501, ahora crea la fila con `business_id: "test-detailing-co"` correcto. Creación de job completo con asignación de empleado vía navegador headless real (Playwright) contra el panel admin corriendo localmente: `POST jobs` devolvió `201` con `assigned_to` y `business_id` ambos correctos — confirma de punta a punta tanto este fix como el de arriba. Datos de prueba (customer + job) borrados después. No se tocó ningún dato de `altalux`/`blissclean` — para esos dos tenants `current_business_id()` sigue resolviendo `'altalux'`/`'blissclean'` exactamente igual que el default viejo, cero cambio de comportamiento.
- **2026-08-08 (2):** Tras el fix del login fresco (ver entrada anterior), Luis seguía reportando "Invalid email or password" con una contraseña que YA había sido confirmada correcta por API y por navegador headless contra el sitio real en vivo (múltiples veces, incluido un `last_sign_in_at` real registrado en Supabase). Causa real encontrada: el campo de email en ambos logins (`admin/index.html`, `technician/index.html`) ya hacía `.trim()`, pero el campo de **password nunca lo hacía** — un espacio invisible al copiar/pegar la contraseña desde un mensaje se manda tal cual a Supabase Auth, que la rechaza sin ninguna pista de que el problema es solo un espacio de más (aparece como el mismo "Invalid email or password" genérico que una contraseña realmente incorrecta). Se agregó `.trim()` al password en los 2 archivos. Empleado real de prueba usado para diagnosticar (con su permiso implícito, cuenta real de producción): `merajuancamilo0913@gmail.com` (business_id `altalux`) — se le reseteó la contraseña más de una vez durante el diagnóstico, dejarle saber la última.
- **2026-08-08:** Luis reportó "no funciona el login" (empleado nuevo, "contraseña incorrecta") en `technician/index.html`. Era el mismo bug del `loadEmployeesFromSupabase()`/login fresco encontrado y corregido el 05 en `admin/index.html` — pero `technician/index.html` es un archivo completamente separado con su propia copia de esa lógica, y quedó explícitamente marcado como "gap conocido, no bloqueante" en el reporte del 05 (se subestimó el impacto). Mismo fix aplicado: recargar `EMPLOYEES` justo después de `signInWithPassword()` exitoso, antes de `findEmployeeByEmail()`. Verificado con un empleado de prueba desechable — login exitoso, `sessionStorage.loggedInName` seteado, la app carga normal ("Good morning, Tech"). Datos de prueba limpiados después.
- **2026-08-05:** **SaaS Onboarding System — arquitectura y diseño completos, sin pagos reales por tenant todavía.** AltaLux pasa de single/dual-tenant a plataforma real: cualquier negocio de detailing puede aplicar por un wizard público (`onboarding/index.html`), Luis (Super Admin, `blisscleanmobilega@gmail.com`) aprueba desde `platform/index.html`, el tenant obtiene su workspace en `/admin?b=<slug>`. Cobros reales por tenant (Square OAuth por-negocio) quedan explícitamente para una fase futura — esta entrega deja todo el andamiaje listo con un guardrail de pagos real.
  - **Migración** `20260805190000_onboarding_system.sql`: columnas nuevas en `business_settings` (`status`, `slug` UNIQUE, `owner_email` UNIQUE, `onboarding_step`, `setup_complete`, `tos_accepted_at`, `approved_at`, `approved_by`, `booking_settings_confirmed`), tablas `service_templates`/`addon_templates` (sembradas vía `INSERT...SELECT` desde el catálogo real de `altalux` — 21 servicios + 10 add-ons, nunca tipeado a mano). **Crítico:** también actualiza `business_settings_public` (la vista que lee `shared/config.js`) para incluir `status` — sin esto, el booking/admin real de AltaLux se habría bloqueado solo en cuanto se desplegara el check de status, porque la vista vieja no tenía esa columna. Se encontró en la revisión del agente Plan antes de tocar nada.
  - **`manage-tenant`** (Edge Function nueva): único punto de acceso cross-tenant. Auth de dos niveles — Super Admin (email hardcodeado) para `stats`/`list_tenants`/`approve_tenant`/`reject_tenant`/`suspend_tenant`/`reactivate_tenant`/`list_templates`/`update_template`; el propio tenant (JWT ↔ `owner_email`) para `generate_slug`/`save_step`/`tenant_details` (auto-resumen del wizard). `approve_tenant` hace 4 pasos secuenciales: status→approved, crea el `employees` Owner, crea/preserva su cuenta de Supabase Auth (nunca resetea el password si ya existe — el owner ya se registró solo en el wizard), copia templates→catálogo real, manda el email de bienvenida.
  - **Payment guard real (no cosmético)** — decisión explícita del usuario tras confirmar que un guard solo-frontend sería evitable con una llamada directa a la API: `square-payment/index.ts` ahora resuelve el `business_id` real de cada cobro (por `job_number` para cobros con job real — cubre automáticamente los cobros en persona de admin/technician sin tocarlos — o `body.businessId` deny-by-default para el depósito nuevo de booking) y rechaza con 403 cualquiera que no sea `altalux`, sin importar `square_enabled`. `booking/index.html` agrega el mismo guard en el frontend (mismo patrón que `isLocalPreview()`) más 1 campo (`businessId`) en el payload existente.
  - **`onboarding/index.html`** (wizard, 2 pasos): el nombre del negocio viaja en el query string del link de confirmación de Supabase (`emailRedirectTo`), no en localStorage (regla del proyecto) — así sobrevive el reload completo que pasa al volver del email.
  - **`platform/index.html`** (panel Super Admin): Dashboard/Tenants/Templates, todo vía `manage-tenant`, nunca una query directa a `business_settings` de otro tenant.
  - **`admin/index.html`**: banner "Complete your setup (X/4)" (ratchet de un solo sentido), pestaña Settings > Payments muestra "Coming soon" para cualquier `business_id !== 'altalux'`, pantalla de bloqueo de login por status (pending/rejected/suspended).
  - **Bug real encontrado y corregido en el camino:** `loadEmployeesFromSupabase()` se llama una sola vez al cargar la página, ANTES de autenticar — con RLS, `anon` no puede leer `employees` en absoluto, así que un login genuinamente nuevo (sin sesión previa en el navegador — exactamente el caso de un owner recién aprobado entrando por primera vez) siempre fallaba con "No active employee profile", aunque las credenciales fueran correctas. Se corrigió recargando empleados justo después de un `signInWithPassword` exitoso. No es un bug introducido hoy, ya existía — solo nunca se había notado porque un navegador con sesión persistida lo esconde.
  - **`shared/config.js`**: nueva prioridad `?b=<slug>` antes del mapa de dominios (para que `altalux.io`, el dominio de la plataforma — distinto de `altaluxdetail.com` — sirva cualquier tenant), `status` viaja como columna más de `settings` sin plumbing extra.
  - **Revisado por un agente Plan antes de implementar** (a pedido del usuario) — encontró y corrigió 2 fallas reales antes de escribir código: el guard de pago original no cubría los cobros en persona de admin/technician (mandan `job.jobNumber` real, nunca `businessId`), y la falta de actualización de `business_settings_public` habría roto producción real. Ver también el hallazgo del `owner_email`/password durante `approve_tenant` (no resetear si ya existe).
  - **Verificado en producción real, sin gastar dinero**, con navegador headless real (Playwright) en cada pieza — no solo `curl`: signup completo (incluida la simulación del redirect de confirmación de email vía hash de sesión real), aprobación completa con verificación en DB de los 4 pasos, guard de pago en sus 3 rutas (job real, depósito nuevo, invoice/pay-link) incluso con `square_enabled` forzado a `true` a mano, panel de Super Admin completo (login de dos niveles, dashboard, tenants, templates), checklist de setup con sus 4 condiciones reales. Los 10 checks de la validation checklist del prompt original pasaron — ver detalle completo en el reporte de esa sesión.
  - **Gap conocido, no bloqueante:** `technician/index.html` (archivo separado, fuera del alcance explícito de esta entrega) no tiene la misma pantalla de bloqueo por status ni el fix del login fresco que sí se aplicó en `admin/index.html`.
- **2026-08-05:** Ícono "📍 Navigate" agregado junto a la dirección en el modal de detalle del job (`admin/index.html`) — abre Google Maps con la dirección como búsqueda de texto libre (los jobs no guardan `place_id`, solo texto). A pedido explícito de Luis, el alcance quedó acotado **solo** al modal del job ("el tiquete del trabajo") — se identificaron otros 4 lugares de solo lectura (visor de invoice ×2, tarjetas de Calendar/horario de empleado) y 2 campos editables (Quick Job/Edit Job) que también muestran `job.address`, pero se dejaron sin tocar a propósito. Reusa el mismo patrón de URL ya usado en la vista de técnico (`?api=1&query=`, botón "📍 Open in Maps" ya existente) y el mismo patrón visual (ícono junto al texto) ya usado en el perfil del cliente (pestaña Addresses).
- **2026-07-22:** Bug real encontrado por Luis en vivo inmediatamente después de subir el fix de recibos PDF: el botón "Download Receipt" daba `{"code":"UNAUTHORIZED_NO_AUTH_HEADER"}` en vez de descargar el PDF. Causa: los botones nuevos usaban `window.open(generate-receipt-pdf?token=...)` — Supabase exige un header `Authorization` con un JWT válido a nivel de gateway **antes** de que el código de la Edge Function corra, y una navegación de navegador normal (`window.open`/`<a href>`) nunca manda ese header, solo `fetch()` puede fijarlo a mano. El caso de `paymentId` (pagos en efectivo) ya usaba `fetch()` + blob + descarga por su propia cuenta de auth y funcionaba bien; se unificó el caso de `token` al mismo patrón en `admin/index.html` y `pay/index.html`. Confirmado el diagnóstico reproduciendo el error exacto sin header, y confirmado el fix con header vía `curl` antes de desplegar.
- **2026-07-22:** **Recibos PDF reales + invoices versionadas + UI colapsable** (extensión del Payment Tracking del mismo día, a pedido de Luis tras revisar el feature en vivo). Tres piezas:
  1. **Invoices versionadas:** `sendPayableInvoice()` en `admin/index.html` ya no reusa/sobreescribe una sola fila de `invoices` por job — ahora cada envío (Send Invoice / Send Payable Invoice) crea una fila nueva, con su propio monto/token/fecha. Antes, reenviar una solicitud con un monto distinto perdía el detalle del envío anterior para siempre. `job.invoiceHistory` (array, todas las solicitudes del job) reemplaza el viejo patrón de sobrescribir `job.invoice` en cada fila cargada; `job.invoice` sigue existiendo como alias de la más reciente para no romper el resto del código. No requirió migración — todas las columnas ya existían.
  2. **Recibos PDF reales** — nueva Edge Function `generate-receipt-pdf`, usa `pdf-lib` vía `esm.sh` (JS puro, sin dependencias nativas — no hay forma de correr un navegador headless dentro de una Edge Function). Antes, "Download as PDF"/"Download Receipt" en todo el sitio eran literalmente `window.print()`, nunca un archivo real. Dos modos: `token` (invoice real, público, mismo modelo que `pay/index.html`) o `paymentId` (pago suelto de Cash/Zelle/Square Reader sin invoice asociada — requiere JWT de empleado activo, validado contra `employees`). El layout replica `invoiceBizInfo()`/`renderInvoiceLineItems()`/`renderInvoiceTotals()` de `admin/index.html`, reimplementado en Deno porque esas funciones manipulan el DOM directamente. Botón "📄 Download Receipt"/"📄" agregado en 3 lugares: Link Activity del modal del job, Payment History del modal del job (por pago suelto), y Job History del perfil del cliente. `pay/index.html` también se actualizó para usar el PDF real en vez de `window.print()`.
  3. **PDF adjunto en el email de notificación al admin** — `square-payment` ahora llama a `generate-receipt-pdf?format=base64` antes de notificar al admin, y `send-email`/`sendViaResend()` pasa el resultado como `attachments` a la API de Resend (antes solo mandaba `from/to/subject/html`, sin soporte de adjuntos). Si la generación del PDF falla, el email se manda igual sin adjunto — nunca bloquea la notificación.
  4. **UI colapsable para el timeline de Link Activity** — con 3+ intentos de pago fallidos la lista se volvía interminable (reportado por Luis con captura real: 3 pares started/failed = 6 líneas sueltas). Nuevo componente reusable `collapsibleGroupHtml()`/`.collapse-group` (mismo patrón visual que el toggle del mini-calendario — chevron que rota 90°), usado en dos niveles: un grupo colapsable por invoice (si el job tiene varias), y dentro de cada una, cada par `payment_started`+`payment_failed` consecutivo se colapsa en 1 línea "❌ Failed attempt" expandible (en vez de 2 líneas sueltas). De paso se corrigió un bug visible en la misma captura: cada apertura del link generaba una línea nueva repitiendo el mismo contador acumulado ("Link opened ×2" duplicado dos veces) — ahora se funde en una sola línea.
  - **Verificado en producción real, sin gastar dinero:** `generate-receipt-pdf` desplegada y probada contra el invoice real de Luis ($1, DISCOVER ****2201) — el PDF generado se revisó visualmente, datos correctos. Modo `paymentId` confirmado que rechaza con 401 sin un JWT de empleado válido. Email `payment_notification_admin` con el PDF adjunto probado de extremo a extremo vía Resend (id de mensaje real devuelto). El agrupamiento de intentos fallidos se verificó manualmente contra los eventos reales que Luis ya había generado en su prueba en vivo (2 link_opened, 3 pares started/failed, 1 completed — exactamente el escenario de su captura) — no se pudo verificar visualmente en el navegador desde este sandbox (mismo límite ya conocido), pendiente que Luis lo confirme.
- **2026-07-22:** **Payment Tracking — trazabilidad completa de pagos en tiempo real.** Antes no había forma de saber si un cliente abrió el link de pago, con qué tarjeta pagó, ni cuándo. Nueva tabla `payment_events` (migración `20260722190000_payment_tracking.sql`, con Realtime habilitado vía `ALTER PUBLICATION supabase_realtime`) registra 4 tipos de evento: `link_opened`, `payment_started`, `payment_completed`, `payment_failed`. Columnas nuevas en `invoices`: `link_opened_at`, `link_open_count`, `payment_started_at`, `card_brand`, `card_last4`, `square_payment_id`. Nueva Edge Function pública `track-payment-event` (valida por `public_token`, rate-limited, nunca revela si un token existe o no). `square-payment` ahora captura marca/últimos 4 dígitos de la tarjeta desde la respuesta de Square (API REST directa → campos snake_case, no el SDK), inserta el evento `payment_completed`, y dispara un email nuevo (`payment_notification_admin` en `send-email`) a `business_settings.notification_email`. `pay/index.html` registra `link_opened` al cargar la factura y `payment_started`/`payment_failed` alrededor del cobro (fire-and-forget, nunca bloquea el pago). `admin/index.html`: nueva sección "Link Activity" en el modal de detalle del job (deliberadamente separada de la sección "Payment History" ya existente, que muestra pagos cobrados, no interacción con el link — mismo modal, dos cosas distintas), indicador de apertura del link junto a "Share Payment Link", y suscripción Realtime (`db.channel`, mismo patrón que ya usaba `bookings`) con toasts en vivo cuando el cliente abre el link o completa el pago.
  - **Hallazgos durante el análisis previo (STEP 0) que corrigieron el plan original:** no existe una tabla `businesses` en este proyecto — `business_id` es `text` en todas las tablas, no uuid con FK a una tabla separada. `square-payment` llama a Square vía REST directo, no el SDK Node — los campos de la respuesta son snake_case (`card_details.card.card_brand`, `last_4`), no camelCase. El SELECT de invoice en `square-payment` no traía `job_id` (se agregó). El id de job usado en el modal de admin (`currentModalJobId`) es un índice local secuencial, no el UUID real de Supabase — hay que usar `job.supabaseId` para cualquier query real. `notification_email` en `business_settings` ya existía desde Fase A (no hacía falta agregarla), pero **estaba vacía en producción** — como efecto colateral, esto significa que el email `internal_notification` (alerta de booking nuevo) probablemente nunca llegó a nadie desde que existe. Se configuró `notification_email = 'altaluxdetail@gmail.com'` (la cuenta real del panel admin) — corrige ambos emails (booking nuevo y pago recibido) a la vez.
  - **Verificado en producción real** (sin gastar dinero ni tocar invoices reales de clientes): `track-payment-event` probado contra un invoice real sin pagar (`link_open_count` subió a 1, `link_opened_at` se seteó, fila real en `payment_events`); `payment_notification_admin` probado con un envío real vía `send-email` (Resend devolvió `id` de mensaje real); un evento `payment_completed` de prueba insertado directo por SQL confirmó que el schema/FKs/CHECK constraint son válidos. Todos los datos de prueba se limpiaron después (`DELETE` del evento, reset de `link_open_count`/`link_opened_at`). **Pendiente real de verificar:** el toast en vivo y el timeline "Link Activity" del modal necesitan una sesión real de admin autenticada — no se pudo probar visualmente desde este sandbox (mismo límite que ya aplicaba a otras features).
  - **Fix de seguimiento el mismo día:** Luis probó el feature en vivo enviando un invoice real de $1 (Job #2, reusando el mismo job de prueba histórico) — el registro sí llegó a Supabase correctamente (confirmado: `sent_at`, `link_opened_at` 20 segundos después, `link_open_count: 1` — el pipeline de tracking funcionó de punta a punta en producción), pero **no se veía el monto en ningún lado de la UI** — ni en "Link Activity" del modal del job, ni en el perfil del cliente (no existía ninguna vista de "solicitudes de invoice enviadas" ahí). Causa: `job.invoice` (el objeto local que arma `loadAllDataFromSupabase()`) nunca guardaba `final_amount`/`title`/`comment` de la fila real de `invoices` — solo status/fechas/token. Corregido: se agregaron esos 3 campos al mapeo, la línea "Invoice sent" de "Link Activity" ahora muestra el monto (`Invoice sent — $1.00`), y se agregó una línea nueva a cada entrada de "Job History" en el perfil del cliente (`buildJobHistoryEntryHtml`) mostrando la solicitud de invoice enviada con su monto y fecha, independientemente de si ya se pagó o no — antes esa vista solo mostraba pagos ya cobrados y el balance pendiente, nunca el evento de "se envió una solicitud por $X".
  - Deploy: migración aplicada con `supabase db push` **antes** de desplegar las Edge Functions (si se hiciera al revés, el primer pago real fallaría con 500 aunque Square ya hubiera cobrado). Las 3 funciones (`track-payment-event`, `square-payment`, `send-email`) desplegadas con `--use-api`. Type-checkeado con `deno check` antes de desplegar — encontró y corrigió un error real (un join anidado `customers(full_name)` se infería como array sin schema generado; se separó en dos queries).
- **2026-07-22:** Auditoría de sincronización repo/GitHub/Hostinger a pedido de Luis, sin cambios de código. Verificado byte a byte que `altalux-hostinger-2026-07-18.zip` (en la carpeta de OneDrive) coincide exactamente con el working tree local en el commit `d59fd53` (los 8 archivos del zip son idénticos al repo) — si ese zip ya se subió a Hostinger, producción está al día. `git fetch origin main` confirmó que `origin/main` también está en `d59fd53`: los 32 commits que un `git status` sin fetch previo mostraba como "ahead of origin/main" ya estaban pusheados, esa referencia local simplemente estaba obsoleta por falta de fetch en la sesión anterior — no había nada pendiente de push.
- **2026-07-18:** El mini-calendario mensual de la vista Calendar (arriba del panel Agenda/Week/Month/Day) ahora arranca colapsado por defecto, en escritorio y móvil — reportado por Luis tras subir el rediseño de Calendar ("aun esta ese calendario grande ahi ocupando todo el espacio"). Se colapsa a una sola fila delgada con el mes/año y una flecha (▸/▾) que lo expande/oculta con un click; al expandirlo reaparecen las flechas de mes anterior/siguiente. No cambia su lógica interna (sigue siendo el mismo `renderMiniCalendar()`), solo el estado de visibilidad inicial y un toggle nuevo.
- **2026-07-18:** Bug real encontrado en producción: dos jobs distintos con el mismo `job_number = 4`. Causa raíz: `admin/index.html` mandaba explícitamente `job_number: job.jobNumber` en los 2 únicos inserts a `jobs` (Quick Job del FAB y "Convert to Job" de Proposals), calculado con un contador en memoria (`nextJobNumCounter`) que arrancaba hardcodeado en `4` y nunca se resincronizaba con lo que ya existía en Supabase tras cargar datos reales — cada sesión nueva volvía a ofrecer "004" como si fuera el primer job. La columna `jobs.job_number` ya tenía su propio default de sequence (`nextval('jobs_job_number_seq')`) pero mandarlo explícitamente lo pisaba, y no había ninguna restricción `UNIQUE` que lo impidiera. Fix: los 2 inserts ya no mandan `job_number` (lo asigna la DB), y el número real vuelto por el insert se sincroniza de vuelta a `job.jobNumber` local (antes el UI mostraba el número optimista para siempre, incluso después de que la DB asignara otro). De paso se corrigió que los jobs cargados desde Supabase mostraban `job_number` sin cero-relleno (ej. "JOB #4" en vez de "JOB #004", inconsistente con los jobs creados en la misma sesión). Migración `20260718150000_fix_duplicate_job_number.sql`: renumeró el job duplicado más reciente (sin invoice enviada todavía) y agregó `UNIQUE (job_number)` sobre `jobs` como salvaguarda para que este bug no pueda repetirse silenciosamente — confirmado en vivo con consulta directa a la DB antes y después.
- **2026-07-18:** Rediseño de Calendar en admin — pasa de mini-calendario + week-grid fijo + 2 listas (jobs/events del día) a un panel con 4 pestañas Agenda/Week/Month/Day + barra de navegación de periodo (ingresos + cantidad de jobs, ±7 días para Agenda/Week, ±1 mes para Month, ±1 día para Day). Las 3 funciones viejas (`renderWeekView`/`renderJobsListForDay`/`renderEventsListForDay`) se dejaron como wrappers de compatibilidad hacia el nuevo `renderCalendarViews()` — evita tener que tocar ~20 puntos del código que ya las llamaban tras crear/editar jobs, proposals o events. Se aprovechó el cambio para conectar los 4 KPI cards de arriba (Today's Revenue/This Week/Jobs Today/Pending), que estaban muertos desde siempre (hardcodeados en $0.00/0). Se agregó el panel "Share Payment Link" (Copy Link + Web Share API) al modal de detalle de job en admin y technician, visible solo si la invoice ya se envió — technician no cargaba `job.invoice` en absoluto hasta este fix (solo lo llenaba en memoria si el envío pasaba por esa misma sesión), ahora también trae las invoices reales desde Supabase al cargar los jobs de la semana.
- **2026-07-18:** Events ahora soporta rangos de fecha/hora y "All Day" (migración `20260718140000_events_date_range.sql`, columnas `end_date`/`end_time`/`all_day`). Sirve para bloquear el calendario varios días seguidos (vacaciones, días libres) o un rango de horas dentro de un día (mantenimiento de equipo). `eventCoversDay()`/`eventCoversSlot()` reemplazan el match exacto de un solo día/slot — probado con 22 casos (todo-el-día, rango en un día, rango multi-día con horas específicas, y compatibilidad con eventos viejos de un solo slot).
- **2026-07-18:** Auditoría completa de "¿qué botón realmente escribe a Supabase?" a pedido de Luis. Encontrado y corregido:
  - `updateJobStatus()` (usada por el selector de status, "Mark Completed", acciones rápidas de Calendar) solo mutaba memoria — ningún cambio de status sobrevivía un reload. Confirmado en vivo con consulta directa a la DB tras el fix.
  - **Proposals y Events no tenían tabla en Supabase** — vivían 100% en arrays de JS (`PROPOSALS`/`EVENTS`), se perdían al recargar. Migración `20260718120000_proposals_events_tables.sql` crea ambas tablas (mismo patrón de RLS que `jobs`). Confirmado en vivo con consulta directa a la DB.
  - Settings > Servicios y Precios solo dejaba editar precio y activo — la columna `description` de `business_services` nunca se exponía en la UI. Ahora hay un textarea editable por fila.
  - Direcciones/vehículos del perfil de cliente (`CUSTOMER_PROFILES`, sistema separado derivado del historial de jobs) nunca escribían a Supabase. Direcciones ahora sincronizan al jsonb `customers.addresses`; vehículos usan la tabla `vehicles` (existía pero nunca se usaba) con insert/update/delete real vía `customer_id` (resuelto por email).
- **2026-07-18:** Bug real encontrado en el catálogo dinámico de precios (del fix de ayer): `business_services.package` para las categorías Interior/Exterior tiene el MISMO valor que `category` (ej. `'interior'`), no `NULL` como se asumió — por eso esas dos categorías nunca actualizaban precios (Full Detail sí, porque ahí `package` sí es real: 'essential'/'premium'). Confirmado en vivo (Exterior mostraba $159.99 hardcodeado vs $169.99 real) antes y después del fix. De paso: `job.invoice.number` guardaba el entero crudo de invoice_number en vez de "INV-2026-0004" al cargar desde Supabase (afectaba Resend Receipt y emails de confirmación/recordatorio) — corregido. Emails: agregada protección contra dark mode de Apple Mail/iPhone (invertía colores sin `color-scheme: light`).
- **2026-07-18:** Edit Job tampoco escribía fecha/status/precio/categoría/notas a la fila real de `jobs` en Supabase (solo lo hacía para nombre/teléfono/email, arreglado el 17) — reportado por Luis como "cambio la fecha de un job y no se guarda". Ahora persiste todo eso también. Pendiente conocido: vehículo/tipo de vehículo (tabla relacional `job_vehicles`/`vehicles`) y add-ons (`job_addons`) todavía no sincronizan desde este modal — se dejó fuera a propósito para no arriesgar una reescritura relacional apurada.
- **2026-07-18:** Bug real encontrado (reportado como "el invoice por correo dice que el link expiró"): `job.invoice.publicToken` nunca se cargaba desde Supabase (solo id/sentAt), y el routing de "Send Invoice" del visor decidía ir a "Resend Receipt" con solo mirar si el job tenía algún pago — pero el depósito del booking se cobra directo con Square y nunca crea una fila en `invoices`. Un job con solo depósito mandaba a Resend Receipt, que armaba el link con `token=null`. Corregido: se captura `public_token` al cargar, y el routing ahora exige que ya exista un invoice real enviado (`job.invoice.publicToken`) antes de ofrecer Resend Receipt.
- **2026-07-17:** 4 fixes reportados desde el uso real del dashboard (Edit Job):
  1. Google Maps autocomplete conectado a `#ej-address` (faltaba, mismo patrón que los demás campos de dirección).
  2. **Catálogo de precios dinámico**: `JOB_CATALOG`/`ADDONS_CATALOG` eran 100% hardcodeados en el código — nunca reflejaban cambios hechos en Settings > Servicios y Precios. Ahora se sobrescriben con los datos reales de `business_services`/`business_addons` al cargar el dashboard (`applyLiveCatalog()`), con fallback a los valores hardcodeados si Supabase no responde. Corrige Edit Job **y** el modal rápido "+ Job" del FAB (comparten el mismo catálogo).
  3. **Bug del checkbox "Override Price" que no se podía desmarcar**: causado por un listener global de `.role-radio-option` (pensado solo para los 4 radio-buttons de rol de empleado) que se aplicaba también a checkboxes sin relación por compartir la misma clase CSS — forzaba `checked=true` en cada click. Acotado a `[data-role]`. Efecto secundario positivo: también arregla el mismo bug latente en "Set as Default" (dirección/vehículo) del perfil de cliente.
  4. **Campos First Name / Last Name** en los 7 lugares donde antes había un solo campo "Name": `booking/index.html` (`c-firstname`/`c-lastname`), y en admin/technician — Edit Job, Quick Job, Create Proposal, Customer Profile, Edit Customer (technician), Add Customer (technician). Para datos existentes con un solo string, se divide con `splitFullName()` (primera palabra = nombre, resto = apellido); al guardar se reconstruye con `joinName()` — el resto de la app sigue usando el nombre combinado sin cambios. **Además**, Edit Job (admin), Customer Profile (admin) y Edit Customer (technician) ahora sí escriben el nombre/teléfono/email a la fila real de `customers` en Supabase vía `job.customerId` — antes esos 3 modales solo mutaban el job en memoria y el cambio se perdía al recargar la página (bug pre-existente, no introducido en esta sesión, corregido a pedido). El modal "Add Customer" de technician (`cf-*`) no se tocó en ese aspecto — nunca creó una fila real en Supabase, solo alimenta el caché local de perfiles.
  - Bug encontrado y corregido en el camino: el grid de 2 columnas de First/Last Name no colapsaba en móvil en 3 modales de admin (cj/cp/profile, usaban un grid inline sin breakpoint) y en los 2 modales de technician (donde se desbordaba fuera del viewport de 420px) — admin ahora reusa `.edit-job-section-grid` (ya tiene el breakpoint de 560px), technician los apila siempre (app 100% móvil).
- **2026-07-17:** Rediseño del invoice imprimible en admin (`admin/index.html`) — documento blanco profesional (logo a color, business info, Bill To / Service Details lado a lado, línea de método de pago) en vez del tema oscuro con overrides de impresión. Se quitó el checkbox "Apply Tax (6%)" (Georgia no cobra tax en detailing móvil) — Subtotal = Total ahora. "Mark as Sent" ahora escribe de verdad a `invoices.sent_at`/`status` en Supabase (con guardia `.is('sent_at', null)` contra doble-click) — antes solo mutaba estado en memoria. "Send Invoice" del visor de invoice ahora abre los modales reales que ya existían (`send-invoice-overlay` si el job no tiene pagos, `resend-receipt-overlay` si ya tiene alguno) en vez de un `alert()` placeholder. Bug encontrado y corregido en el camino: el `position:absolute` del contenedor de impresión estaba anidado dentro de `.modal-box`, lo que colapsaba su altura a la del padre en vez de la página completa — se ancló en `#invoice-modal-overlay` (hijo directo de body) en su lugar. Los 7 templates de email en `supabase/functions/send-email/index.ts` también se rediseñaron con el mismo sistema visual (tarjetas con acento lateral, media query mobile, Rajdhani/Inter) — desplegado con `supabase functions deploy send-email`.
- **2026-07-17:** Google Maps Places autocomplete extendido a `booking/index.html` (`#c-address`) y al modal "Quote/Job" de admin (`#cj-address`) — mismo patrón de fallback (`gm_authFailure`) que el modal de Customer. Reusa la misma key ya restringida a `app.altaluxdetail.com/*`, sin cambios necesarios en Google Cloud. Probado en vivo en `admin` (New Customer) tras habilitar "Places API" clásica en el proyecto de Google Cloud (estaba restringida en la key pero nunca habilitada a nivel de proyecto — dos pasos separados, causaba `LegacyApiNotActivatedMapError`).
- **2026-07-17:** Nuevo modal "New Customer" en `admin/index.html` (Person/Business, múltiples direcciones/teléfonos/emails, Google Places autocomplete). Migración `20260717180000_customers_extended.sql` aplicada. Google Maps API key restringida por HTTP referrer (`app.altaluxdetail.com/*`) y por API (solo Places + Maps JavaScript) en Google Cloud Console — hecho manualmente por Luis, yo no tengo acceso a esa consola. Nota: el happy-path del autocompletado (dropdown de sugerencias) no se pudo probar end-to-end en el sandbox de desarrollo porque la key rechaza el origen `file://` (`RefererNotAllowedMapError`, esperado) — sí se probó y confirmó el fallback (input vuelve a texto editable si Maps falla) y todo el resto del modal (toggle, filas múltiples, validación, guardado). Probado y confirmado funcionando en vivo en `app.altaluxdetail.com`.
- **2026-07-17:** `technician/index.html` — el panel de detalle de job (`#job-detail-screen`) se ocultaba con `transform:translateX(100%)`, relativo a su propio ancho (máx. 430px). En viewports más anchos que 430px (laptop/tablet) esto solo lo desplazaba 430px sin sacarlo de la pantalla, dejando un panel vacío visible junto al login. Corregido a `translateX(100vw)` (mueve el ancho completo del viewport, sin cambiar el comportamiento en móvil). Commit `b13d164`.

## Seguridad — cambios importantes (2026-07-10 y 2026-07-13)
- Se eliminó el modelo de contraseñas en texto plano (`employees.password`); cada empleado usa una cuenta real de Supabase Auth.
- Se eliminó la cuenta maestra de Supabase Auth que antes estaba embebida en el código fuente de `admin/index.html` (visible por "Ver código fuente", con acceso completo de lectura/escritura a la DB).
- Nueva Edge Function `manage-employee-auth` para crear/resetear logins sin exponer passwords al cliente.
- **2026-07-13:** typo de una letra en el Square Application ID (`z` minúscula en vez de `Z` mayúscula) causaba 401 Unauthorized y el formulario de pago de Square no cargaba en booking/admin/technician. Corregido (commit `e2f45b8`).

## Clon SaaS independiente (altalux.io) — completo y en vivo (2026-08-10)
Infraestructura 100% separada de AltaLux/este repo, desplegada y verificada end-to-end: repo `blisscleanga-svg/altalux-saas`, proyecto Supabase propio (`bgbjrmkgjhjnvyffpxiu`), 7 Edge Functions desplegadas, Hostinger sirviendo `altalux.io` con el código nuevo. `altalux-app`/`altaluxdetail.com` (este repo) no se tocó — cero cambios de código de producto, solo estas dos entradas de contexto. **Este archivo ya no trackea el desarrollo de la plataforma SaaS de acá en adelante** — ver `altalux-saas/CONTEXT.md` para eso. Detalle completo de cómo se hizo (incluidos varios hallazgos importantes: 8 tablas core que nunca existieron como migración, un bug de grants, fugas de datos de AltaLux limpiadas) en `docs/superpowers/plans/2026-08-09-saas-clone-separation.md` y `.superpowers/sdd/progress.md`.

## Pendiente / Próximos pasos
- [ ] Envío real de invoices/confirmaciones por email (Resend) — hoy son placeholders "coming soon"
- [ ] Twilio: el Toll-Free ya está aprobado y `twilio_phone = '+18883300491'`. Falta que el controlador (a) revise el diff del fix de code review final del 2026-09-02, (b) despliegue `send-sms` y `twilio-webhook`, (c) suba el zip nuevo a Hostinger (`admin/` + `booking/` cambiaron), (d) vuelva a poner `twilio_enabled = true` para `altalux`, y (e) haga el round-trip real de STOP/START sobre el número vivo para confirmar que el opt-out de Twilio y `sms_opt_outs` quedan de acuerdo
- [ ] Fase 5 Sub-fases 2–4: deep-link a chat SMS desde el detalle de un job, "On My Way" (geolocalización/ETA), campañas/broadcast manual
- [ ] Fase 6: adaptar multi-tenant para BlissClean
- [ ] Confirmar que Hostinger sirve la última versión del sitio tras cada fix
- [ ] Mover el Personal Access Token de GitHub fuera de un archivo de texto plano
- [ ] El deploy a Hostinger sigue siendo manual vía zip (`C:\Users\ledua\OneDrive\Escritorio\Altaluxapp\altalux-hostinger-YYYY-MM-DD.zip`), no automático desde git — cada fix en el repo requiere generar y subir un zip nuevo aparte. Confirmado el 2026-07-17: el zip del 2026-07-15 estaba al día con el repo salvo por el fix de `job-detail-screen` de esa misma fecha (17).
- [ ] Se encontró `token subapse.txt` en texto plano en esa misma carpeta de OneDrive (`Altaluxapp/`) — mover/eliminar igual que el PAT de GitHub, no debería vivir como archivo de texto sin cifrar.
- [ ] Esa carpeta de OneDrive tiene varias copias de deploy (`altalux-deploy`, `altalux-deploy-2026-07-15`, `Nueva carpeta`) — vale la pena consolidar para no confundir cuál es la vigente.

## Estructura de Carpetas
```
altalux-app/
├── booking/index.html       # Widget de booking embebible
├── admin/index.html         # Panel de administración
├── technician/index.html    # App del técnico
├── pay/index.html           # Página pública de pago de invoices (?token=)
├── supabase/
│   ├── migrations/          # ver supabase/migrations/ — timestamped desde 2026-07-17
│   └── functions/
│       ├── square-payment/          # Cobro real vía Square Payments API + refunds del flujo de invoice
│       ├── square-refund/           # Reembolsos reales vía Square Refunds API
│       ├── send-email/              # Resend — 8 tipos de email (ver Cuentas y Servicios)
│       ├── track-payment-event/     # NUEVO (2026-07-22) — pública, registra link_opened/payment_started/payment_failed
│       └── manage-employee-auth/    # Alta/reset de logins (solo Owner, service role)
└── CONTEXT.md                # Este archivo
```

## Servicios AltaLux (confirmar precios con Luis)
- Exterior Detail
- Full Detail
- Paint Correction
- Ceramic Coating

## Cuentas y Servicios
- **Square:** cuenta activa, credenciales de producción (Application ID, Location ID `LEWG2XNWRA7BS`). El access token vive como variable de entorno (`SQUARE_ACCESS_TOKEN`) en la Edge Function, nunca en el código.
- **Supabase:** proyecto activo (`xmhsehfdmiqbwhpqjgon`), Auth real por empleado, 2 Edge Functions en uso.
- **Resend:** cuenta por crear/conectar.
- **Twilio:** Toll-Free **aprobado**, `+1 888-330-0491`, ya seteado en `business_settings.twilio_phone` para `altalux`. `twilio_enabled` está en **`false`** a propósito desde el 2026-09-02: el controlador lo pausó al encontrarse el relay abierto de `send-sms` en el code review final, y lo vuelve a encender él después de revisar el fix (ver "Fixes recientes"). Código/infraestructura lista (schema, `send-sms`/`twilio-webhook`, Message Center en admin); las dos Edge Functions tienen cambios sin desplegar.
- **Hostinger:** activo — altaluxdetail.com.
- **GHL:** activo (toll-free +1 888-853-0590).

## Contacto / Owner
- Luis Pabón — Founder, BlissClean & AltaLux
- Equipo: Dario (técnico), Ami
- Sitio: altaluxdetail.com

## Identidad Visual AltaLux
- Azul principal: #104872
- Naranja: #FF8C00
- Dorado: #FFAA00
- Blanco: #FFFFFF
- Fondo oscuro UI: #0a1628
- Tipografía web: Rajdhani (títulos) + Inter (cuerpo)
- Logo archivo: /shared/assets/logo-altalux.png
